import { trip } from './trip';
import { surprise } from './surprise';
import { flightInfo } from './flightInfo';
import { brand } from './brand';
import { bag } from './bag';

export const schemaTypes = [trip, surprise, flightInfo, brand, bag];
